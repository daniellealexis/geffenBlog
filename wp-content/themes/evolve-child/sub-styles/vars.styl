/*
	Contains all variables to be used across all stylus files
	Also includes mixins and animations
*/

// Colors
$navBlue = #273039
$navDarkBlue = #1b2228

$lightGrey = #c1c1c1


// @media Values
$menuBreakpoint = 768px


// Mixins
flexContainer()
    display -webkit-flex
    display -ms-flexbox
    display flex
    -webkit-flex-wrap wrap
    -ms-flex-wrap wrap
    flex-wrap wrap
